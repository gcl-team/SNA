namespace SimNextgenApp.Configurations;

/// <summary>
/// Represents static configuration settings that might be associated 
/// with a type of load (entity) in the simulation.
/// </summary>
public record LoadStaticConfig : IStaticConfig
{
    // This record is intentionally empty for now.
    // Future static properties common to a type of load could be added here.
}